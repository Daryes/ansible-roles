# {{ ansible_managed }}

listen_addresses = '{{ postgresql_listen_address |replace('0.0.0.0','*') }}'
port = {{ postgresql_listen_port}}

# extra settings from postgresql_settings_raw[]
{{ postgresql_config_settings_raw |default([]) |flatten |join('\n') }}

