# {{ ansible_managed }}

# Set the command-line arguments to pass to the server.
ARGS='{{ prometheus_alertmanager_service_extra_args }} --web.listen-address="{{ prometheus_alertmanager_listen_ip }}:{{ prometheus_alertmanager_listen_port }}"  --config.file="{{ role_prometheus_dist.conf }}/alertmanager.yml"  --web.config.file=/etc/prometheus/web-config.prometheus.yml --storage.path="{{ prometheus_data_dir }}/{{ role_prometheus_dist.alertmanager_data_subdir }}/"'

# use -help to list all supported parameters

