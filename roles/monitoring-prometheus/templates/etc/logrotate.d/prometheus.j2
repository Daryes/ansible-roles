# {{ ansible_managed }}

{{ role_prometheus_dist.log }}/prometheus.log {
    daily
    rotate {{ prometheus_syslog_history_days }}
    copytruncate

    missingok
    notifempty

    compress
    delaycompress

    dateext
    dateyesterday
}
